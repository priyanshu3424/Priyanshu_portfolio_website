import React from 'react';

const Header = () => {
  return (
    <header className="header">
      {/* The navigation menu has been permanently removed */}
    </header>
  );
};

export default Header;